# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
---
include:
  - board: nice_nano//zmk
    shield: swweeep_left nice_view_adapter nice_view
  - board: nice_nano//zmk
    shield: swweeep_right nice_view_adapter nice_view
  # nRF54LM20 DK touchpad bring-up. Builds against the L20 PDK target
  # (already supported in v4.1.0+zmk-fixes) as a stand-in for LM20.
  - board: nrf54l20pdk/nrf54l20/cpuapp
    shield: tps65_dk
