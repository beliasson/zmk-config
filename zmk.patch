diff --git a/app/CMakeLists.txt b/app/CMakeLists.txt
index bf5cc29d..8cc6f871 100644
--- a/app/CMakeLists.txt
+++ b/app/CMakeLists.txt
@@ -101,7 +101,6 @@ target_sources_ifdef(CONFIG_ZMK_HID_INDICATORS app PRIVATE src/events/hid_indica
 target_sources_ifdef(CONFIG_ZMK_SPLIT app PRIVATE src/events/split_peripheral_status_changed.c)
 add_subdirectory_ifdef(CONFIG_ZMK_SPLIT src/split)
 target_sources_ifdef(CONFIG_ZMK_SPLIT app PRIVATE src/events/split_peripheral_layer_changed.c)
-add_subdirectory(src/split)
 
 target_sources_ifdef(CONFIG_USB_DEVICE_STACK app PRIVATE src/usb.c)
 target_sources_ifdef(CONFIG_ZMK_USB app PRIVATE src/usb_hid.c)
diff --git a/app/src/keymap.c b/app/src/keymap.c
index b574fe07..0532c79a 100644
--- a/app/src/keymap.c
+++ b/app/src/keymap.c
@@ -18,7 +18,7 @@ LOG_MODULE_DECLARE(zmk, CONFIG_ZMK_LOG_LEVEL);
 #include <zmk/sensors.h>
 #include <zmk/virtual_key_position.h>
 #if IS_ENABLED(CONFIG_ZMK_SPLIT_BLE)
-#include <zmk/split/bluetooth/central.h>
+#include <zmk/split/central.h>
 #endif
 
 #include <zmk/event_manager.h>
diff --git a/app/src/rgb_underglow.c b/app/src/rgb_underglow.c
index 81928437..7e8d71b6 100644
--- a/app/src/rgb_underglow.c
+++ b/app/src/rgb_underglow.c
@@ -28,6 +28,7 @@
 #include <zmk/events/activity_state_changed.h>
 #include <zmk/events/usb_conn_state_changed.h>
 #include <zmk/events/underglow_color_changed.h>
+#include <zmk/events/layer_state_changed.h>
 #include <zmk/workqueue.h>
 #include <zmk/events/split_peripheral_layer_changed.h>
 
@@ -74,7 +75,6 @@ struct rgb_underglow_state {
     uint8_t current_effect;
     uint16_t animation_step;
     bool on;
-    bool layer_enabled;
 };
 
 static const struct device *led_strip;
@@ -207,43 +207,42 @@ static int zmk_rgb_underglow_apply_rgbmap(const struct zmk_behavior_binding *bin
                                           size_t rgbmap_len) {
     int rc = 0;
     for (int i = 0; i < STRIP_NUM_PIXELS; i++) {
-        uint8_t midx = rgb_pixel_lookup(i);
-        if (midx >= ZMK_KEYMAP_LEN) {
-            LOG_DBG("out of range");
-        } else {
-            const struct device *dev = zmk_behavior_get_binding(bindings[midx].behavior_dev);
+        int midx = rgb_pixel_lookup(i);
+        if (midx < 0 || midx > ZMK_KEYMAP_LEN) {
+            continue;
+        }
+        const struct device *dev = zmk_behavior_get_binding(bindings[midx].behavior_dev);
 
-            if (dev == NULL) {
-                continue;
-            }
+        if (dev == NULL) {
+            continue;
+        }
 
-            const struct behavior_driver_api *api = (const struct behavior_driver_api *)dev->api;
+        const struct behavior_driver_api *api = (const struct behavior_driver_api *)dev->api;
 
-            if (api->binding_pressed == NULL) {
-                continue;
-            }
-            struct zmk_behavior_binding_event event = {.position = midx,
-                                                       .timestamp = k_uptime_get()};
+        if (api->binding_pressed == NULL) {
+            continue;
+        }
+        struct zmk_behavior_binding_event event = {.position = midx,
+                                                    .timestamp = k_uptime_get()};
 
-            int color = api->binding_pressed((struct zmk_behavior_binding *)&bindings[midx], event);
+        int color = api->binding_pressed((struct zmk_behavior_binding *)&bindings[midx], event);
 
-            if (color > 0) {
-                pixels[i] =
-                    hex_to_rgb((color & 0xFF0000) >> 16, (color & 0xFF00) >> 8, color & 0xFF);
-                rc = 1;
-            } else {
-                pixels[i] = (struct led_rgb){r : 0, g : 0, b : 0};
-            }
+        if (color > 0) {
+            pixels[i] =
+                hex_to_rgb((color & 0xFF0000) >> 16, (color & 0xFF00) >> 8, color & 0xFF);
+            rc = 1;
+        } else {
+            pixels[i] = (struct led_rgb){r : 0, g : 0, b : 0};
         }
     }
     return rc;
 }
 
-static void zmk_rgb_underglow_set_layer(uint8_t layer) {
-    if (!state.layer_enabled)
+static void zmk_rgb_underglow_update_layer() {
+    if (state.current_effect != UNDERGLOW_EFFECT_LAYER_INDICATORS)
         return;
 
-    const struct zmk_behavior_binding *rgbmap = rgb_underglow_get_bindings(layer);
+    const struct zmk_behavior_binding *rgbmap = rgb_underglow_get_bindings(rgb_underglow_top_layer());
     if (rgbmap != NULL && zmk_rgb_underglow_apply_rgbmap(rgbmap, ZMK_KEYMAP_LEN)) {
         if (!state.on)
             zmk_rgb_underglow_transient_on();
@@ -253,6 +252,11 @@ static void zmk_rgb_underglow_set_layer(uint8_t layer) {
             zmk_rgb_underglow_transient_off();
     }
 }
+
+static void zmk_rgb_underglow_effect_layer() {
+    // zmk_rgb_underglow_set_layer(rgb_underglow_top_layer());
+}
+
 #endif /* IS_ENABLED(UNDERGLOW_LAYER_ENABLED) */
 
 static void zmk_rgb_underglow_tick(struct k_work *work) {
@@ -269,6 +273,9 @@ static void zmk_rgb_underglow_tick(struct k_work *work) {
     case UNDERGLOW_EFFECT_SWIRL:
         zmk_rgb_underglow_effect_swirl();
         break;
+    case UNDERGLOW_EFFECT_LAYER_INDICATORS:
+        zmk_rgb_underglow_effect_layer();
+        break;
     }
 
     int err = led_strip_update_rgb(led_strip, pixels, STRIP_NUM_PIXELS);
@@ -280,7 +287,7 @@ static void zmk_rgb_underglow_tick(struct k_work *work) {
 K_WORK_DEFINE(underglow_tick_work, zmk_rgb_underglow_tick);
 
 static void zmk_rgb_underglow_tick_handler(struct k_timer *timer) {
-    if (!state.on || state.layer_enabled) {
+    if (!state.on) {
         return;
     }
 
@@ -373,17 +380,12 @@ int zmk_rgb_underglow_get_state(bool *on_off) {
     if (!led_strip)
         return -ENODEV;
 
-    *on_off = state.on || state.layer_enabled;
+    *on_off = state.on;
     return 0;
 }
 
 int zmk_rgb_underglow_on(void) {
     zmk_rgb_underglow_transient_on();
-#if IS_ENABLED(UNDERGLOW_LAYER_ENABLED)
-    if (state.current_effect == UNDERGLOW_EFFECT_LAYER_INDICATORS) {
-        state.layer_enabled = true;
-    }
-#endif
     return zmk_rgb_underglow_save_state();
 }
 
@@ -418,7 +420,6 @@ K_WORK_DEFINE(underglow_off_work, zmk_rgb_underglow_off_handler);
 
 int zmk_rgb_underglow_off(void) {
     zmk_rgb_underglow_transient_off();
-    state.layer_enabled = false;
     return zmk_rgb_underglow_save_state();
 }
 
@@ -457,9 +458,10 @@ int zmk_rgb_underglow_select_effect(int effect) {
 
     state.current_effect = effect;
     state.animation_step = 0;
-#if IS_ENABLED(UNDERGLOW_LAYER_ENABLED)
-    state.layer_enabled = (effect == UNDERGLOW_EFFECT_LAYER_INDICATORS);
-#endif
+
+    if (state.current_effect == UNDERGLOW_EFFECT_LAYER_INDICATORS) {
+        zmk_rgb_underglow_update_layer();
+    }
     return zmk_rgb_underglow_save_state();
 }
 
@@ -578,7 +580,7 @@ static int rgb_underglow_auto_state(bool target_wake_state) {
 
     if (sleep_state.is_awake) {
 #if IS_ENABLED(UNDERGLOW_LAYER_ENABLED)
-        zmk_rgb_underglow_set_layer(rgb_underglow_top_layer());
+        zmk_rgb_underglow_update_layer();
         return 0;
 #else
         if (sleep_state.rgb_state_before_sleeping) {
@@ -602,21 +604,19 @@ static int rgb_underglow_event_listener(const zmk_event_t *eh) {
 #endif
 
 #if IS_ENABLED(UNDERGLOW_LAYER_ENABLED)
-    if (as_zmk_split_peripheral_layer_changed(eh)) {
-        const struct zmk_split_peripheral_layer_changed *ev =
-            as_zmk_split_peripheral_layer_changed(eh);
+    const struct zmk_split_peripheral_layer_changed *ev =
+        as_zmk_split_peripheral_layer_changed(eh);
+    if (ev != NULL) {
         LOG_DBG("zmk_split_peripheral_layer_changed: %08x", ev->layers);
 #if !IS_ENABLED(CONFIG_ZMK_SPLIT_ROLE_CENTRAL)
         set_peripheral_layers_state(ev->layers);
 #endif
-        uint8_t layer = rgb_underglow_top_layer();
-        LOG_DBG("top layer: %d", layer);
-        zmk_rgb_underglow_set_layer(layer);
+        zmk_rgb_underglow_update_layer();
         return 0;
     }
-    if (as_zmk_underglow_color_changed(eh)) {
+    if (as_zmk_underglow_color_changed(eh) || (IS_ENABLED(CONFIG_ZMK_SPLIT_ROLE_CENTRAL) && as_zmk_layer_state_changed(eh))) {
         LOG_DBG("refresh layer");
-        zmk_rgb_underglow_set_layer(rgb_underglow_top_layer());
+        zmk_rgb_underglow_update_layer();
         return 0;
     }
 #endif /* UNDERGLOW_LAYER_ENABLED */
@@ -644,6 +644,9 @@ ZMK_SUBSCRIPTION(rgb_underglow, zmk_usb_conn_state_changed);
 #endif
 
 #if IS_ENABLED(UNDERGLOW_LAYER_ENABLED)
+#if IS_ENABLED(CONFIG_ZMK_SPLIT_ROLE_CENTRAL)
+ZMK_SUBSCRIPTION(rgb_underglow, zmk_layer_state_changed);
+#endif
 ZMK_SUBSCRIPTION(rgb_underglow, zmk_split_peripheral_layer_changed);
 ZMK_SUBSCRIPTION(rgb_underglow, zmk_underglow_color_changed);
 #endif
diff --git a/app/src/rgb_underglow_layer.c b/app/src/rgb_underglow_layer.c
index 54642941..8d6ff2cd 100644
--- a/app/src/rgb_underglow_layer.c
+++ b/app/src/rgb_underglow_layer.c
@@ -40,7 +40,13 @@ const int pixel_lookup_table[] = DT_INST_PROP(0, pixel_lookup);
 
 static int zmk_rgbmap_ids[ZMK_RGBMAP_LAYERS_LEN] = {DT_INST_FOREACH_CHILD_SEP(0, LAYER_ID, (, ))};
 
-const int rgb_pixel_lookup(int idx) { return pixel_lookup_table[idx]; };
+const int rgb_pixel_lookup(int idx) { 
+    if (idx < 0 || idx > ARRAY_SIZE(pixel_lookup_table)){
+        return -ENOENT;
+    }
+    
+    return pixel_lookup_table[idx];
+};
 
 const int zmk_rgbmap_id(uint8_t layer) {
     for (uint8_t i = 0; i < ZMK_RGBMAP_LAYERS_LEN; i++) {
